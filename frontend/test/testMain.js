const expect = require('chai').expect;
let chai = require('chai');
let assert = require('assert');

describe('-- APP TESTS -- ', () => {
  describe('we should write hell of a lot of tests', () => {
    it('just works', () => {
      assert.ok(true);
    });
  });
});
